// Noop entry. This fixture exists for static scanning by the
// iterion secured-renovacy bot's security_audit node — node-ipc is
// listed as a dependency to trip the protestware advisory but is
// never actually loaded.
console.log('protestware-fixture noop');
