apiVersion: v2
name: iterion
description: |
  Iterion workflow orchestration engine — Kubernetes deployment.
  Server (HTTP API + editor SPA) plus an optional runner pool that
  pulls work from NATS JetStream and persists state to MongoDB + S3.
type: application
# Chart `version` and `appVersion` are both kept in sync with package.json
# by scripts/sync-chart-version.sh (release-it after:bump hook) so that
# the OCI artifact published by .github/workflows/release.yml carries the
# same semver as the iterion binary it deploys. Consumers pin via
# `helm install ... --version <semver>`.
version: 0.7.0
appVersion: "0.7.0"
home: https://github.com/SocialGouv/iterion
sources:
  - https://github.com/SocialGouv/iterion
maintainers:
  - name: SocialGouv
keywords:
  - workflow
  - orchestration
  - llm
  - iterion
icon: https://raw.githubusercontent.com/SocialGouv/iterion/main/build/appicon.png

# Sub-charts are bundled to make `task chart:kind` self-contained:
# values-dev.yaml turns them on, values-prod.yaml leaves them off so
# operators bring their own MongoDB Atlas / S3 / managed NATS.
dependencies:
  - name: mongodb
    version: "16.5.x"
    repository: oci://registry-1.docker.io/bitnamicharts
    condition: mongodb.enabled
  - name: nats
    version: "1.2.x"
    repository: https://nats-io.github.io/k8s/helm/charts/
    condition: nats.enabled
  - name: minio
    version: "14.7.x"
    repository: oci://registry-1.docker.io/bitnamicharts
    condition: minio.enabled
